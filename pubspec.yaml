name: baselineconnect
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  shimmer: ^1.0.0
  localstorage:
  msgraph:
  aad_oauth:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  assets:
    - assets/user.jpg
    - assets/data.json
  uses-material-design: true
